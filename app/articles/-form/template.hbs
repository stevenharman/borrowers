<form>
  <h2>{{errorMessage}}</h2>
  <fieldset>
    {{input value=model.description}}<br/>
    {{input value=model.notes}}<br/>
    <button {{action 'save'}} class="primary">Save</button>
    <button {{action 'cancel'}}>Cancel</button>
  </fieldset>
</form>
